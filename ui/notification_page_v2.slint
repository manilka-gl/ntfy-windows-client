import {
    Button,
    CheckBox,
    ComboBox,
    HorizontalBox,
    VerticalBox,
} from "std-widgets.slint";
import { FieldLabel, Theme } from "components_v2.slint";

export component NotificationSettingsPage inherits Rectangle {
    in-out property <bool> notifications-enabled;
    in-out property <bool> sound-enabled;
    in-out property <int> placement-index;
    in-out property <int> audio-output-index;
    in property <[string]> audio-outputs;
    callback save-settings();
    callback refresh-audio-outputs();

    background: #00000000;

    Text {
        x: 0px;
        y: 0px;
        width: parent.width;
        height: 30px;
        text: "Notifications";
        color: Theme.text;
        font-size: 23px;
        font-weight: 700;
        vertical-alignment: center;
    }

    Text {
        x: 0px;
        y: 34px;
        width: parent.width;
        height: 20px;
        text: "Configure popup visibility, sound output, and screen placement.";
        color: Theme.secondary;
        font-size: 12px;
        vertical-alignment: center;
    }

    Rectangle {
        x: 0px;
        y: 70px;
        width: parent.width;
        height: 350px;
        border-radius: 12px;
        background: Theme.panel;
        border-width: 1px;
        border-color: Theme.border;

        VerticalBox {
            x: 18px;
            y: 16px;
            width: parent.width - 36px;
            height: parent.height - 32px;
            spacing: 8px;

            HorizontalBox {
                height: 40px;
                CheckBox {
                    text: "Show desktop popup";
                    checked <=> root.notifications-enabled;
                }
                Rectangle { horizontal-stretch: 1; }
                CheckBox {
                    text: "Play sound";
                    checked <=> root.sound-enabled;
                }
            }

            Rectangle {
                height: 1px;
                background: Theme.border;
            }

            FieldLabel { text: "AUDIO OUTPUT"; }
            HorizontalBox {
                height: 38px;
                spacing: 8px;

                ComboBox {
                    horizontal-stretch: 1;
                    model: root.audio-outputs;
                    current-index <=> root.audio-output-index;
                    enabled: root.sound-enabled;
                }

                Button {
                    width: 88px;
                    text: "Refresh";
                    clicked => { root.refresh-audio-outputs(); }
                }
            }

            FieldLabel { text: "POPUP POSITION"; }
            ComboBox {
                height: 38px;
                model: [
                    "Top left", "Top centre", "Top right",
                    "Middle left", "Centre", "Middle right",
                    "Bottom left", "Bottom centre", "Bottom right"
                ];
                current-index <=> root.placement-index;
            }

            Rectangle {
                height: 58px;
                border-radius: 8px;
                background: #121a24;
                border-width: 1px;
                border-color: Theme.border;

                Text {
                    x: 12px;
                    y: 7px;
                    width: parent.width - 24px;
                    height: 18px;
                    text: "Popup stays inside the Windows work area";
                    color: Theme.text;
                    font-size: 11px;
                    font-weight: 650;
                    vertical-alignment: center;
                }

                Text {
                    x: 12px;
                    y: 27px;
                    width: parent.width - 24px;
                    height: 23px;
                    text: "Placement uses physical display bounds so the complete notification remains visible.";
                    color: Theme.muted;
                    font-size: 10px;
                    wrap: word-wrap;
                    vertical-alignment: center;
                }
            }

            HorizontalBox {
                height: 34px;
                Rectangle { horizontal-stretch: 1; }
                Button {
                    width: 112px;
                    text: "Save settings";
                    clicked => { root.save-settings(); }
                }
            }
        }
    }
}
